service: rbac-project-root
frameworkVersion: '3'

provider:
  name: aws
  stage: ${opt:stage, 'dev'}
  region: ${opt:region, env:AWS_REGION, 'us-east-1'}

# Infrastructure stacks were moved to:
# - infrastructure/dynamodb.yml
# - infrastructure/serverless.yml

resources:
  Description: Root stack intentionally empty. Deploy from infrastructure/ configs.
